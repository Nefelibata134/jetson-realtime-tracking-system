# 变更记录

本文件记录项目的重要变更。

## [未发布]

### 新增

- 可选择的 GStreamer x264 标注视频输出，使用适合 Orin Nano CPU 的低延迟编码配置，
  并支持显式码率控制。
- 在运行指标和完整流水线基准分组中加入编码器与码率字段。
- CAVIAR 公开场景外部事件验证协议，固定三组开发/留出视频，并提供带 SHA-256 校验的
  下载、MPEG2 到 H.264 MP4 转换、人工规则冻结、事件真值生成、RTSP 实时回放和
  Precision/Recall/F1/证据完整率评估工具。
- 留出运行要求显式 `--allow-holdout`；未完成人工复核的规则不能生成真值或启动评估。
- 冻结首个 CAVIAR 评估周期的双向穿线、3 秒停留和入口 ROI 规则，并保留独立的待复核
  模板用于后续评估周期。
- 在任何系统推理前确认 `Browse_WhileWaiting2` 的冻结停留 ROI 位置正确且全程无人进入，
  将其保留为零事件负样本；另按 CAVIAR 官方场景说明预先加入未查看结果的 `Browse2`
  作为正样本留出片段，规则与阈值保持不变。
- 人工确认 `Browse2` 的冻结 ROI 仍表达合理的展台停留区，并确认其唯一预期停留事件发生
  于 `23.48 s`；增加语义审计与真值审计两道程序门禁，防止未复核留出片段启动运行。
- 完成四段留出视频共 7 个预期事件的人工真值审核，并明确 ROI 事件采用“启动占用即告警”
  语义；单元测试固定连续两帧确认后的启动占用事件。
- CAVIAR Nano 正式留出结果、代表性事件截图和逐段机器可读汇总。
- Nano、Tiny 与 S 在同一冻结开发规则下的事件对比，以及 YOLOX-S 官方 640x640 engine
  构建、探测和板端基准工具。
- YOLO26s 官方 v8.4.0 权重元数据、固定 SHA-256、Ultralytics 8.4.138 one-to-many ONNX
  导出工具和目标 Jetson FP16 engine 构建说明；模型资产和生成物继续保留在仓库外。
- 许可证与发布边界文档，区分项目自有源码、仓库内第三方源码、外部 SDK、模型资产和
  历史版本授权。
- 统一的分段计时检测器接口与显式 YOLO26s 工厂入口；独立 RGB/255 居中 letterbox、
  one-to-many `cxcywh` 解码、按类别 NMS 和静态张量契约检查。
- YOLO26 模式要求显式检测/跟踪阈值；实时入口增加可配置的匹配阈值，原默认值不变。
  MOT 序列工具新增独立预处理、TensorRT 和后处理 P95，保留原检测路径计时字段。
- MOT17/CAVIAR 评估脚本的显式 YOLO26 入口、必填阈值检查与校准/开发序列限制；旧 YOLOX
  数值默认值不变，新增运行参数和文件哈希记录，拒绝覆盖已有 MOT 结果目录。
- CAVIAR 在计分前强制检查目标帧数、零丢帧、零帧序缺口和无重启；无效轮次保留但不计分。

### 变更

- 完整流水线基准默认使用 x264，同时保留 MP4V 作为兼容性基线。
- 操作者选择功率模式并执行 `jetson_clocks` 后，基准的功率模式和锁频校验不再要求
  root 权限。
- README、工程文档与自动生成报告统一使用中文说明，同时保留命令、指标字段和运行契约。
- YOLOX 检测器从 TensorRT engine 静态契约解析输入尺寸，不再把所有候选固定为 416x416。
- systemd 服务默认 engine 从 YOLOX-Nano 切换为开发集事件 F1 更高的 YOLOX-Tiny；Nano
  结果继续作为不可改写的历史基线，S 保留为候选。
- 从许可证迁移提交开始，项目自有源码及包含兼容组件的整体项目分发采用 `AGPL-3.0-only`，
  并须满足其源码提供和分发义务；截至并包括提交
  `d790926b0187d27b1f5f5607f1ef709c909eaf7f` 的历史 MIT 授权继续有效，第三方组件
  文件本身未被重新许可，保留各自原始许可和版权声明。外部专有 SDK 不随仓库分发，
  未来二进制或捆绑发布前须重新审计。

### 验证

- 在 25W 与 MAXN_SUPER 锁频模式下，x264 后端均能以 1280x720 和 1920x1080 写入
  600/600 个标注帧，输出零丢帧。GStreamer 检查确认每个 600 帧测量文件均为
  30 FPS、20 秒的 H.264 视频。
- CAVIAR Nano 正式留出完整处理 `4,310/4,310` 帧，输入丢帧和序列缺口均为 0；事件
  TP/FP/FN 为 `2/1/5`，Precision `66.67%`、Recall `28.57%`、F1 `40.00%`，精度门禁失败。
- 同一 CAVIAR 开发规则下 Nano、Tiny 与 S 的 F1 分别为 `47.06%`、`53.33%` 和
  `50.00%`；已消费留出没有用于候选调参或重新声明独立测试。
- YOLOX-S 720p 完整流水线在 25W 与 MAXN_SUPER 下均达到 `30.03 FPS`、零测量丢帧和
  x264 `600/600`，但事件 F1 未超过 Tiny，因此未提升为默认模型。
- YOLO26s 固定权重在主机上两次导出均得到 `[1,84,8400]` one-to-many 输出和相同 ONNX
  SHA-256；该检查只验证导出可重复性，不形成质量评估结论。
- YOLO26 C++ 主机确定性检查覆盖归一化、颜色、补边、坐标还原、NMS、异常张量与公共接口；
  不依赖 TensorRT 或 GPU。
- 提交 `49876fe` 在 Jetson 25W 下通过 TensorRT 编译/链接、FP16 engine 构建、零张量探测
  和合成灰图检测，原服务恢复且配置与默认 Tiny engine 哈希不变，详见
  [板端最小验证](docs/benchmarks/yolo26s_jetson_smoke.md)。真实视频质量、固定 A/B、候选
  完整流水线实时性与长期稳定性尚未验证，不代表模型升级完成。

## [1.0.0] - 2026-08-23

### 新增

- 通过 GStreamer 接入文件、IMX219 CSI 和 H.264 RTSP。
- 有界丢弃最旧帧采集队列，支持无帧检测和有限次数输入源恢复。
- YOLOX-Nano 与 YOLOX-Tiny 预处理、TensorRT 执行、解码和 NMS。
- 按类别关联的 ByteTrack，包含 Kalman 预测、两阶段关联、丢失轨迹老化和流重置语义。
- ROI 入侵、有限线段定向穿越和基于时间戳的停留事件。
- 版本化 JSONL 事件日志、原子截图和有界事件前后片段。
- 带显式背压指标的异步标注视频输出。
- 版本化原子运行指标与后台 Jetson 遥测。
- systemd readiness、真实帧 watchdog、有序停止、保留策略和日志轮转。
- 可复现的 Jetson 流水线基准、MOT17 评估、1 小时服务持续运行及进程/输入源故障注入。

### 验证

- 在 25W 与 MAXN_SUPER 锁频配置下完成 720p 和 1080p 完整流水线测试。
- 使用固定 TrackEval 的 MOT17 公开训练序列留出划分。
- 60 分钟 CSI 服务持续运行，以及受控主进程和 RTSP 中断。

### 已知限制

- TensorRT plan 不能跨 Jetson 型号或 TensorRT/CUDA 软件栈移植，因此必须在目标设备生成。
- 随项目提供的 COCO 预训练 YOLOX 模型没有针对特定部署场景微调。
- 在实测设备上，OpenCV MP4V 软件编码无法保存全部 1080p/30 标注帧；有界输出队列通过
  丢弃过时输出帧保护分析主链路。
- 为保持证据顺序，截图和 JSONL 发布仍为同步操作，慢存储会提高事件帧延迟。片段和标注
  视频编码使用有界后台工作线程。
- RTSP URI 作为进程参数传入；本地进程查看工具可能看到 URI 中嵌入的凭据。
- 事件证据保存在本地，本版本不包含远程上传、认证和设备集群管理。

[1.0.0]: https://github.com/Nefelibata134/jetson-realtime-tracking-system/releases/tag/v1.0.0
